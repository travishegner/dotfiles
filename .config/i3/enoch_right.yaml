modules:
  - name: ShellCommand
    config:
      refresh: 5000
      cmd: "echo \"$(playerctl metadata artist) - $(playerctl metadata title)\""
  - name: ShellCommand
    config:
      refresh: 300000
      cmd: "echo \"$(/home/thegner/tools/bin/archzfs_sync.sh)\""

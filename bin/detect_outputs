#!/bin/bash

outputs=($(xrandr | grep ' connected'| awk '{print $1}' | tr -d '-' | sort))

xrandr --output $(xrandr | grep ' connected' | awk '{print $1}' | sort | head -1) --primary

for o in "${!outputs[@]}"; do
	eval $( echo ${outputs[$o]}=$(( $o + 1 )) )
	export $(echo ${outputs[$o]})
done
